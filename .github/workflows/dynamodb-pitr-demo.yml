name: DynamoDB PITR POC

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment (dev, uat, prod)"
        required: true
        default: "dev"
        type: choice
        options: [dev, uat, prod]

permissions:
  contents: read
  id-token: write

env:
  AWS_REGION: us-east-1
  BASE_TABLE_NAME: poc-dynamodb-pitr-demo

jobs:
  dynamodb-poc:
    runs-on: ubuntu-22.04
    environment:
      name: ${{ github.event.inputs.environment }}

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.OIDC_ROLE }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install boto3
        run: pip install boto3

      - name: Run DynamoDB Create/Update POC with Retry
        env:
          ENVIRONMENT: ${{ github.event.inputs.environment }}
          BASE_TABLE_NAME: ${{ env.BASE_TABLE_NAME }}
          AWS_REGION: ${{ env.AWS_REGION }}
        run: |
          python3 - <<'EOF'
          import boto3, os, time

          ENV = os.getenv("ENVIRONMENT")
          REGION = os.getenv("AWS_REGION")
          BASE_TABLE_NAME = os.getenv("BASE_TABLE_NAME")
          TABLE_NAME = f"{BASE_TABLE_NAME}-{ENV}"

          dynamodb = boto3.client("dynamodb", region_name=REGION)

          # ---------- Helpers ----------
          def table_exists(name):
              try:
                  dynamodb.describe_table(TableName=name)
                  return True
              except dynamodb.exceptions.ResourceNotFoundException:
                  return False

          def wait_for_status(name, desired, retries=60, interval=5):
              for _ in range(retries):
                  try:
                      desc = dynamodb.describe_table(TableName=name)
                      status = desc["Table"]["TableStatus"]
                      if status == desired:
                          print(f"✅ Table {name} reached {desired}")
                          return
                      print(f"⏳ Waiting for {desired} (current: {status})...")
                      time.sleep(interval)
                  except Exception as e:
                      print(f"⚠️ Error while waiting for status: {e}")
                      time.sleep(interval)
              print(f"❌ Timeout waiting for table {name} to reach {desired}.")

          def enable_pitr_with_retry(name, retention_days=7, max_attempts=2):
              for attempt in range(1, max_attempts + 1):
                  try:
                      print(f"🔁 Enabling PITR (Attempt {attempt}/{max_attempts})...")
                      dynamodb.update_continuous_backups(
                          TableName=name,
                          PointInTimeRecoverySpecification={
                              "PointInTimeRecoveryEnabled": True,
                              "RecoveryPeriodInDays": retention_days
                          }
                      )
                      print(f"✅ PITR enabled on {name} ({retention_days}-day retention).")
                      return True
                  except dynamodb.exceptions.ContinuousBackupsUnavailableException:
                      if attempt < max_attempts:
                          print(f"⏳ PITR not ready (attempt {attempt}). Retrying in 10s...")
                          time.sleep(10)
                      else:
                          print(f"❌ PITR unavailable after {max_attempts} attempts.")
                  except Exception as e:
                      print(f"⚠️ PITR enable error: {e}")
                      return False
              return False

          # ---------- Main Logic ----------
          def create_or_update_table():
              table_def = {
                  "TableName": TABLE_NAME,
                  "BillingMode": "PAY_PER_REQUEST",
                  "AttributeDefinitions": [{"AttributeName": "id", "AttributeType": "S"}],
                  "KeySchema": [{"AttributeName": "id", "KeyType": "HASH"}],
              }

              if not table_exists(TABLE_NAME):
                  print(f"🚀 Creating table: {TABLE_NAME}")
                  if ENV in ("prod", "prd"):
                      table_def["DeletionProtectionEnabled"] = True
                      print("🔒 Deletion protection enabled for production.")

                  dynamodb.create_table(**table_def)
                  wait_for_status(TABLE_NAME, "ACTIVE")

                  # Enable PITR only for production
                  if ENV in ("prod", "prd"):
                      print("📦 Enabling Point-in-Time Recovery (PITR)...")
                      enable_pitr_with_retry(TABLE_NAME)
                  else:
                      print("ℹ️ Non-prod environment — skipping PITR and deletion protection.")

                  # TTL enablement (non-breaking)
                  try:
                      print("⏰ Enabling TTL on 'ttl_expire_at'...")
                      dynamodb.update_time_to_live(
                          TableName=TABLE_NAME,
                          TimeToLiveSpecification={"Enabled": True, "AttributeName": "ttl_expire_at"},
                      )
                      print("✅ TTL enabled.")
                  except Exception as e:
                      print(f"⚠️ TTL enable error: {e}")

              else:
                  print(f"ℹ️ Table {TABLE_NAME} already exists — checking current configuration...")

                  # --- Check PITR ---
                  if ENV in ("prod", "prd"):
                      try:
                          desc = dynamodb.describe_continuous_backups(TableName=TABLE_NAME)
                          pitr_desc = desc["ContinuousBackupsDescription"]["PointInTimeRecoveryDescription"]
                          status = pitr_desc.get("PointInTimeRecoveryStatus")
                          retention = pitr_desc.get("RecoveryPeriodInDays", None)

                          if status == "ENABLED":
                              print(f"✅ PITR already enabled ({retention or 'default'} days). Skipping.")
                          else:
                              print(f"⚙️ Enabling PITR (currently disabled)...")
                              enable_pitr_with_retry(TABLE_NAME)
                      except Exception as e:
                          print(f"⚠️ PITR check error: {e}")
                  else:
                      print("ℹ️ Non-prod environment — skipping PITR check.")

                  # --- Check TTL ---
                  try:
                      ttl = dynamodb.describe_time_to_live(TableName=TABLE_NAME)
                      ttl_status = ttl.get("TimeToLiveDescription", {}).get("TimeToLiveStatus")
                      if ttl_status != "ENABLED":
                          print("⚙️ TTL not enabled — enabling now...")
                          dynamodb.update_time_to_live(
                              TableName=TABLE_NAME,
                              TimeToLiveSpecification={"Enabled": True, "AttributeName": "ttl_expire_at"},
                          )
                          print("✅ TTL re-enabled.")
                      else:
                          print("✅ TTL already enabled.")
                  except Exception as e:
                      print(f"⚠️ TTL check error: {e}")

          # ---------- Execute ----------
          create_or_update_table()
          print(f"🎉 POC complete — check DynamoDB console for table: {TABLE_NAME}")
          EOF
